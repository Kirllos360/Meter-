# Symbiot Bridge Integration — v2.0.0

## Architecture Overview

The Symbiot bridge connects field meter hardware to the NestJS API server via 10 dedicated TCP channels (ports 5010–5019). Each channel supports up to 100 concurrent HTTP connections, yielding a theoretical maximum of 1,000 concurrent meter connections.

## Bridge Models

| Model | Description | Use Case |
|-------|-------------|----------|
| **Per-Area** | One bridge instance per geographic area | Isolated deployments, WAN segmentation |
| **Centralized** | Single bridge managing all channels | Single-datacenter, low-latency LAN |

Both models are supported and configurable via `config/bridge.config.ts`.

## TCP Channel Specification

- **Port Range:** 5010 – 5019 (10 channels)
- **Connections per channel:** 100 max concurrent
- **Framing:** 4-byte big-endian length prefix + JSON payload
- **Protocol:** JSON-over-TCP with UTF-8 encoding
- **TLS:** TLS 1.3 mandatory between bridge and API server (mutual certificate auth optional)

## Health Check & Failover

- **Health check interval:** 5 seconds (not 30s — reduced for faster detection)
- **Dead channel detection:** 3 missed health checks = channel marked dead (~15s)
- **Auto-failover:** Traffic rerouted to healthy channels within 1 cycle
- **Reconnection backoff:**
  ```
  Attempt 1: 1s
  Attempt 2: 2s
  Attempt 3: 4s
  Attempt 4: 8s
  Attempt 5+: 30s (cap)
  ```

## Quarantine System

Readings outside acceptable thresholds are moved to a quarantine table instead of being dropped:

```
Meter Reading → Validation → [PASS] → MeterReadings (production)
                            → [FAIL] → QuarantineReadings (review)
```

Quarantined readings are flagged with a reason code and can be manually reviewed or auto-released after re-validation.

## Windows Service Management

The bridge runs as a Windows Service via `node-windows`:

| Command | Action |
|---------|--------|
| `npm run bridge:install` | Install Windows service |
| `npm run bridge:start` | Start the service |
| `npm run bridge:stop` | Stop the service |
| `npm run bridge:uninstall` | Remove the service |
| `npm run bridge:restart` | Restart the service |

Service name: `MeterSymbiotBridge`
Display name: `Meter Symbiot Bridge v2.0.0`

## Message Protocol

### Frame Structure

```
┌──────────────────────────────┐
│        4 bytes (uint32)      │  ← Payload length (big-endian)
├──────────────────────────────┤
│        N bytes (UTF-8)       │  ← JSON payload
└──────────────────────────────┘
```

### Message Envelope

```json
{
  "messageId": "uuid-v4",
  "type": "meter_reading | health_check | ack | error | config_sync",
  "timestamp": "2026-06-13T12:00:00.000Z",
  "channel": 0,
  "payload": { }
}
```

## Mermaid Diagrams

### Bridge Request Lifecycle (Sequence Diagram)

```mermaid
sequenceDiagram
    participant Meter as Field Meter
    participant Bridge as Symbiot Bridge
    participant TCP as TCP Channel Pool
    participant Quarantine as Quarantine Service
    participant API as NestJS API Server
    participant DB as PostgreSQL

    Meter->>Bridge: Raw reading (RS-485/Wi-Fi)
    Bridge->>Bridge: Validate & frame with length prefix
    Bridge->>TCP: Write framed JSON over channel
    TCP->>API: TLS 1.3 encrypted message
    API->>API: Deserialize & validate schema
    alt Valid reading
        API->>DB: INSERT into meter_readings
        API-->>TCP: ACK (messageId, status=ok)
        TCP-->>Bridge: Forward ACK
        Bridge-->>Meter: Success response
    else Corrupt/outlier reading
        API->>Quarantine: INSERT into quarantine_readings
        API-->>TCP: ACK (messageId, status=quarantined, reason)
        TCP-->>Bridge: Forward ACK
        Bridge-->>Meter: Quarantine warning
    else Error
        API-->>TCP: Error frame (code, message)
        TCP-->>Bridge: Forward error
        Bridge-->>Meter: Error response
    end
```

### Bridge Architecture Diagram

```mermaid
graph TB
    subgraph "Field Layer"
        M1[Water Meter]
        M2[Gas Meter]
        M3[Electric Meter]
    end

    subgraph "Bridge Layer - Windows Server 2022"
        BS[Bridge Service<br/>node-windows]
        CM[Channel Manager]

        subgraph "TCP Channels 5010-5019"
            CH0[Ch 0]
            CH1[Ch 1]
            CH2[Ch 2]
            CH3[Ch 3]
            CH4[Ch 4]
            CH5[Ch 5]
            CH6[Ch 6]
            CH7[Ch 7]
            CH8[Ch 8]
            CH9[Ch 9]
        end

        subgraph "Health & Resilience"
            HC[Health Check<br/>5s interval]
            FD[Failover Detector<br/>15s timeout]
            QB[Quarantine Buffer]
            EB[Exponential Backoff<br/>1s → 30s]
        end

        subgraph "Connection Pool"
            CP[100 conn/channel<br/>max = 1000]
            TLS[TLS 1.3]
        end
    end

    subgraph "API Layer - Linux Ubuntu 22.04"
        API[NestJS API Server]
        VAL[Validation Service]
        QRS[Quarantine Service]
    end

    subgraph "Data Layer"
        DB[(PostgreSQL<br/>meter_readings)]
        QDB[(PostgreSQL<br/>quarantine_readings)]
    end

    M1 --> BS
    M2 --> BS
    M3 --> BS
    BS --> CM
    CM --> CH0
    CM --> CH1
    CM --> CH2
    CM --> CH3
    CM --> CH4
    CM --> CH5
    CM --> CH6
    CM --> CH7
    CM --> CH8
    CM --> CH9
    CH0 --> TLS
    CH1 --> TLS
    CH2 --> TLS
    CH3 --> TLS
    CH4 --> TLS
    CH5 --> TLS
    CH6 --> TLS
    CH7 --> TLS
    CH8 --> TLS
    CH9 --> TLS
    TLS --> API
    API --> VAL
    VAL --> QRS
    VAL --> DB
    QRS --> QDB
    HC --> FD
    FD --> EB
    EB --> CM
    QB --> QRS
```

## Configuration (`config/bridge.config.ts`)

```typescript
export const bridgeConfig = {
  mode: 'centralized', // 'centralized' | 'per-area'
  channels: {
    startPort: 5010,
    count: 10,
    maxConnectionsPerChannel: 100,
  },
  healthCheck: {
    intervalMs: 5000,
    missedThreshold: 3,
    failoverEnabled: true,
  },
  reconnection: {
    initialDelayMs: 1000,
    maxDelayMs: 30000,
    multiplier: 2,
  },
  tls: {
    version: 'TLSv1.3',
    mutualAuth: false,
    certPath: './certs/bridge-cert.pem',
    keyPath: './certs/bridge-key.pem',
    caPath: './certs/ca-cert.pem',
  },
  quarantine: {
    enabled: true,
    autoReleaseAfterMs: 86400000, // 24h
  },
};
```
